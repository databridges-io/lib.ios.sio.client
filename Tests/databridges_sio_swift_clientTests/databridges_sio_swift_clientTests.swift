import XCTest
@testable import databridges_sio_swift_client

final class databridges_sio_swift_clientTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
       // XCTAssertEqual(databridges_sio_swift_client().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
